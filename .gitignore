# Dataset (large; provided in the hackathon bundle, not committed)
data/candidates.jsonl
data/candidates.jsonl.gz
data/*.json
# ...but DO ship the tiny sample so the Docker sandbox has input to run on.
!data/sample_candidates.json

# Pre-computed embedding artifacts ARE committed (spec 10.3): the no-network Stage-3
# reproduction of the ranking step depends on them. ~76 MB float16 + ids.
# Regenerate with scripts/precompute_embeddings.py if needed.

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Outputs
submission.csv
*.out
data/*.docx

# Generated submission upload files (regenerable from run_submission.py)
/*.csv
