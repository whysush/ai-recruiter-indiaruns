"""AI Recruiter — ranked candidate shortlisting for the Redrob challenge.

A structured-evidence scorer with one genuine semantic component. The package is
deliberately small and inspectable: every term in the final score lives in a named
module so the reasoning we hand a recruiter is faithful to what the code computed.
"""

__version__ = "1.0.0"
